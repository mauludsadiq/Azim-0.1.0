import("std/fs") as fs
import("packages/azim_code/hybrid_loop_v2") as loop

loop.run_hybrid_loop_v2({
  out_root: "out/hybrid_ds_r1",
  candidate_dir: "out/hybrid_ds_r1/candidates",
  run_dir: "out/hybrid_ds_r1/runs",
  accepted_out: "out/hybrid_ds_r1/accepted",
  merged_out: "out/hybrid_ds_r1/merged",
  train_checkpoint_dir: "out/full_corpus_training",
  checkpoint_path: "out/full_corpus_training/code_checkpoint.json",
  base_shard_path: "out/full_corpus_with_hybrid.jsonl",
  tasks: [
    "Write a python class Stack that implements push, pop, peek, and is_empty",
    "Write a python class Queue that implements enqueue, dequeue, peek, and is_empty",
    "Write a python class LinkedList that implements append, prepend, delete, and search",
    "Write a python class DoublyLinkedList that implements append, prepend, delete, and search",
    "Write a python class BinaryTree that implements insert, search, and inorder traversal",
    "Write a python class MinHeap that implements insert, extract_min, and heapify",
    "Write a python class MaxHeap that implements insert, extract_max, and heapify",
    "Write a python class HashTable that implements put, get, and delete with chaining",
    "Write a python class Graph that implements add_edge, bfs, and dfs",
    "Write a python class Trie that implements insert, search, and starts_with",
    "Write a python class DisjointSet that implements union and find with path compression",
    "Write a python class PriorityQueue that implements push and pop by priority",
    "Write a python class Deque that implements append, appendleft, pop, and popleft",
    "Write a python class CircularBuffer that implements fixed-size circular buffer",
    "Write a python class LRUCache that implements get and put with O(1) operations",
    "Write a python class BSTMap that implements put, get, delete as a binary search tree",
    "Write a python class AVLTree that implements insert with self-balancing",
    "Write a python class SegmentTree that implements range sum query and point update",
    "Write a python class FenwickTree that implements prefix sum and point update",
    "Write a python class SparseTable that implements range minimum query in O(1)"
  ],
  langs: ["python"],
  api_url: "https://api.openai.com/v1/chat/completions",
  api_key: fs.read_text(".openai_key"),
  model: "gpt-4o-mini",
  max_azim_attempts: 1,
  max_llm_attempts: 3,
  azim_tokens: 64,
  azim_k: 4,
  azim_temp: 1.0,
  lr: 0.0005,
  epsilon: 0.0001,
  max_train_records: -1,
  steps_per_record: 10,
  min_accepted: 1,
  min_records: 1,
  require_loss_decrease: true
})
